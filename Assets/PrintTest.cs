using Firebase.Auth;
using Firebase.Database;
using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class PrintFriendIdOnClick : MonoBehaviour
{
    /*
    [SerializeField]
    private Button printFriendIdButton;

    private RequestEntry requestUser;

    private void Awake()
    {
        requestUser = GetComponent<RequestEntry>();
    }

    void Start()
    {
        // Asignar el listener al botón
        printFriendIdButton.onClick.AddListener(PrintFriendId);
    }

    private void PrintFriendId()
    {
        if (requestUser != null && !string.IsNullOrEmpty(requestUser._uid))
        {
            Debug.Log($"Friend ID: {requestUser._uid}");
        }
        else
        {
            Debug.LogError("RequestEntry or friend ID is not set.");
        }
    }*/
}

// Suponiendo que RequestEntry es algo así:
/*public class RequestEntry : MonoBehaviour
{
    public string _uid;
    // Aquí podrían ir más datos y métodos relacionados con la entrada de solicitud
}
*/